# SysWatch - Monitor de Recursos del Servidor
# Dependencias Python

# Monitorización del sistema (CPU, RAM, Disco, Red, Procesos)
psutil>=5.9.0

# Servidor web + WebSockets (equivalente a websockets del ejercicio de clase)
flask>=3.0.0
flask-socketio>=5.3.6
flask-cors>=4.0.0

# Motor async de SocketIO (modo threading para compatibilidad con Python stdlib)
simple-websocket>=1.0.0
